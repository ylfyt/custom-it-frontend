query Product($productId: String!) {
	product(id: $productId) {
		id
		name
		description
		price
		stock
		storeId
		imageUrl
		__typename
		comments {
			id
			text
			createAt
			user {
				id
				username
			}
		}
	}
}
